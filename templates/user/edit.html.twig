{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-body">
            <h1 class="card-title">Edit User</h1>

            {{ include('user/_form.html.twig', {'button_label': 'Update'}) }}

            <div class="row mt-3">
                <div class="col">
                    <a href="{{ path('app_user_index') }}" class="btn btn-outline-primary me-2">Back to list</a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
